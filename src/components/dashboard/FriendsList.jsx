import React from "react";
import { styled } from "@mui/system";

const FriendsListContainer = styled("div")({
  flexGrow: 1,
  width: "100%",
});

const FriendsList = () => {
  return <FriendsListContainer></FriendsListContainer>;
};

export default FriendsList;
